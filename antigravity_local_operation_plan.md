# Anti-Gravity-Compatible Local Operation Plan

## Purpose

This document defines the safe local-only workflow for the authenticated Google Anti-Gravity CLI workspace at `/home/ubuntu/instagram_work`. It may be used as a planning reference only. It does not authorize external service access.

## Daily Local Workflow

At 08:00 Asia/Kolkata, run the following bounded local steps.

1. Read the current safety notice and stop if browser-based Instagram activity, external login, publication, sharing, upload, account modification, or credential use is requested.
2. Verify local source MP4 files, completed edit MP4 files, captions, catalog rows, and checksum files. A source counts as complete only when a matching local edit and integrity record exist.
3. If verified unprocessed local source files exist, process no more than five into separate non-public 9:16 professional MP4 drafts. Preserve the original source files unchanged.
4. Create no more than one English educational 60-second draft per day. Use research packets already available locally, or record a blocker when a validated local source packet is unavailable. Do not provide diagnosis, treatment, or individualized health advice.
5. Validate video dimensions and audio tracks. Update only local captions, catalogs, manifests, audit records, and backup snapshots.
6. Report only meaningful local progress or blockers.

## Non-Negotiable Safety Boundaries

The workflow must never browse, log in to, scrape, upload to, publish to, share to, message through, boost, edit, replace, delete, hide, or otherwise interact with Instagram, Facebook, Julius, GitHub, Google, or any other external service. It must never access credentials, passwords, one-time codes, recovery codes, or browser sessions.

## Current Status

The local schedule is active. The Google Anti-Gravity CLI is authenticated but its installed headless mode cannot independently run internal commands because its command-permission prompt is unavailable in headless execution. This plan therefore records compatible workflow rules; the recurring local schedule remains the execution mechanism.
